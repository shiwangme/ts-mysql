// import { pool } from '../src';
// import config from './_config';

// const client = pool(config);

// test('query', async (t: ExecutionContext): Promise<void> => {
//   const rows = await client.query('SELECT 1');
//   t.is(rows.length, 1);
// });
